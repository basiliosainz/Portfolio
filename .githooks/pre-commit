#!/bin/sh
# Regenerates every project page + the nav menu from media/ before each
# commit, so adding/editing a project folder never needs a manual
# `npm run build` - it just happens when you commit.
repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root" || exit 1

node scripts/build.mjs

git add index.html contact.html *.html
